# VisNet 詳細ドキュメント

## 概要
VisNet (`HRNetJointVisibilityNet`) は、各関節に対してオクルージョン確率を推定する軽量モジュールです。Transformer に渡す前の特徴を再評価し、不可視関節のノイズを抑える目的で導入されています。元論文「Rethinking Visibility in Human Pose Estimation: Occluded Pose Reasoning via Transformers」の実装では、Transformer と SimDR ヘッドの間に配置され、疑似ラベルを含む可視性学習を補助します。

**環境要件**: 本リポジトリにおける VisNet の学習・推論はすべて CUDA 対応 GPU 環境を前提とします。CPU のみではサポート対象外です。

## 入力と前処理
- 元の姿勢モデルから取得する特徴は `B × 17 × 64 × 48` のヒートマップテンソルです。
- `view` で各関節を `64 × 48 = 3072` 次元のベクトルに変換し、テンソル形状は `B × 17 × 3072` になります。
- `permute(0, 2, 1)` により `B × 3072 × 17` に整列し、1×1 畳み込みがチャネル軸に作用するよう準備します。

## ネットワーク構成と次元遷移
1. `Conv1d(3072 → 64, kernel=1)` + ReLU: 高次元特徴を圧縮し、関節ごとの情報をダウンサンプリングします。WHY: 3072 次元をそのまま扱うと計算負荷が高く過学習しやすいため、1×1 畳み込みでチャネル混合を簡潔に行います。
2. `Conv1d(64 → 32, kernel=1)` + ReLU: さらなる圧縮でノイズを削ぎ、重要情報に集中させます。
3. `Conv1d(32 → 1, kernel=1)`: 関節ごとのスカラーに射影し、オクルージョン確率の生値を得ます。
4. `view(-1, 17)` で `B × 17` に整形後、`sigmoid` を適用し可視性確率 (0〜1) を出力します。

## 推論時の処理フロー
- 出力確率を `pred_visibility` として取得し、`>= 0.5` などの閾値でマスクに変換します。
- マスクは Transformer/GAU に渡す特徴に乗算して不可視部を抑制します。WHY: 可視性の低い関節は位置推定に悪影響を与えるため、重み付けで影響度を制御します。

## 学習時の損失
- データセットの visibility ラベル（例: COCO の `is_visible`）を `transform_visibility` で整形し、クロスエントロピー損失を計算します。
- 位置回帰損失と同時に最適化することで、姿勢推定と可視性推定の両方を協調学習します。

## 他モデルへの転用指針
- 入力特徴が `B × 関節数 × 特徴次元` の形式であれば、1×1 畳み込みの入出力チャネルを調整するだけで適用できます。
- 出力確率をソフトマスクとして利用すると勾配が滑らかで学習が安定しやすく、ハードマスクとの効果比較が推奨されます。
- 既存重みを転用する場合は `visibility_state_dict` を読み込み、まずは凍結して効果検証した後、必要に応じて微調整すると安全です。
